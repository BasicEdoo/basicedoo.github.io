# 90s Themed Site

This repository contains a simple static website built using only **HTML**, **CSS** and **JavaScript**.

Open `index.html` in your browser to see bright colors and retro fonts reminiscent of the 1990s. The page loads `style.css` for styling and `script.js` for smooth navigation.
